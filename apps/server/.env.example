# Copy to .env and edit. Required.

# Bearer token every client must send (set the same value in the app's Sync
# settings). Generate a strong one:
#   openssl rand -hex 32
IRIS_TOKEN=change-me-to-a-long-random-string

# Optional: host port to publish. The container always listens on 8787.
# IRIS_PORT=8787
